import scala.scalajs.js

object test8 {
  val alist = collection.immutable.HashMap(0 -> 1, 2 -> 3)
  val aseq = Array(1,2,3).to[collection.immutable.Seq]
  val aset = collection.immutable.HashSet(0,1,2)

  def main(args: Array[String]): Unit = {
  }
}
